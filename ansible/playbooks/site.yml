- name: Provision web_server (Docker + ship app container)
  hosts: web_server
  gather_facts: true
  become: true
  vars:
    docker_users:
      - ubuntu
    docker_install_compose_plugin: true
  roles:
    - geerlingguy.docker
    - ship-app